<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="styles.css">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        button, input {
            border: none;
            outline: none;
            text-decoration: none;
            background: none;
        }
        .nav {
            width: 100%;
            height: 4rem;
            position: fixed;
            top: 0;
            display: flex;
            justify-content: space-between;
            background: #fff;
            align-items: center;
            z-index: 1000;
            padding: .5rem 1.3rem;
            box-shadow: 0px 0px 3px 0px rgba(0,0,0, .3);
        }

        .nav-logo {
            width: 45px;
            margin-left: 1rem;
        }

        .nav-titulo {
            font-size: 2rem;
            color: rgba(0,0,0, .9);
            font-weight: 700;
            font-style: italic;
        }

        .login-form {
            display: flex;
            column-gap: .5rem;
        }

        .login-input {
            border: 1px solid rgba(0,0,0, .3);
            padding: 5px 10px;
            transition: .4s;
        }

        .login-input:focus{
            border: 1px solid rgb(0,0,0);
        }

        .login-btn, .logout-btn {
            background: rgb(30,30,30);
            color: #fff;
            font-size: 1rem;
            padding: .4rem .5rem;
            cursor: pointer;
            transition: .4s;
        }

        .login-btn:hover, .logout-btn:hover {
            background: rgb(75,75,75);
            color: #fff;
            text-decoration: none;

        }

         .login-input, .login-btn, .logout-btn {
            border-radius: 5px;
        }
    </style>
    <title>Document</title>
</head>
<body>
    <header style="width: 100%">
        <nav class="nav">
            <a href="index.php"><img class="nav-logo" src="public/imagenes/logo.png"></a>
            <h1 class="nav-titulo">Pokedex!</h1>
            <div style="display: flex; column-gap: 1rem">
                <form class="login-form" method="post" action="../login.php">
                    <input class="login-input" type="text" name="usuario" placeholder="Usuario" >
                    <input class="login-input" type="password" name="contraseña" placeholder="Contraseña" >
                    <button class="login-btn" type="submit" >Ingresar</button>
                </form>
                <a href="../logout.php" class="logout-btn">Salir</a>
            </div>
        </nav>
    </header>
</body>
</html>


