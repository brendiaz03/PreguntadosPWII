<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="styles.css">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        button, input {
            border: none;
            outline: none;
            text-decoration: none;
            background: none;
        }
        .nav {
            width: 100%;
            height: 4rem;
            position: fixed;
            top: 0;
            display: flex;
            justify-content: space-between;
            background: #fff;
            align-items: center;
            z-index: 1000;
            padding: .5rem 1.3rem;
            box-shadow: 0px 0px 3px 0px rgba(0,0,0, .3);
        }

        .nav-logo {
            width: 45px;
            margin-left: 1rem;
        }

        .nav-titulo {
            font-size: 2rem;
            color: rgba(0,0,0, .9);
            font-weight: 700;
            font-style: italic;
        }

        .login-form {
            display: flex;
            column-gap: .5rem;
        }

        .login-input {
            border: 1px solid rgba(0,0,0, .3);
            padding: 5px 10px;
            transition: .4s;
        }

        .login-input:focus{
            border: 1px solid rgb(0,0,0);
        }

        .login-btn, .logout-btn {
            background: rgb(30,30,30);
            color: #fff;
            font-size: 1rem;
            padding: .4rem .5rem;
            cursor: pointer;
            transition: .4s;
        }

        .login-btn:hover, .logout-btn:hover {
            background: rgb(75,75,75);
            color: #fff;
            text-decoration: none;
        }

         .login-input, .login-btn, .logout-btn {
            border-radius: 5px;
        }
    </style>
    <title>Document</title>
</head>
<body>
{{^user}}
    <header style="width: 100%">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">
                <img src="public/imagenes/logo-preguntados.png" width="30" height="30" class="d-inline-block align-top" alt="">
                Preguntados
            </a>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                            <img src="public/imagenes/profile-user" width="30" height="30" class="d-inline-block align-top" alt="">
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                            <i class=""></i>
                        </a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
{{/user}}
</body>
</html>


